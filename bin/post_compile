#!/usr/bin/env bash

echo "-----> Running post-compile hook - minimal setup only"

# Only create directory structure, no downloads at build time
echo "-----> Creating minimal directory structure"
mkdir -p CodeFormer

echo "-----> Post-compile complete (all downloads moved to runtime)"
