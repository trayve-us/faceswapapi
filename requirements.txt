# Minimal build-time dependencies to avoid out-of-memory during build
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
Pillow==10.0.1
numpy==1.21.1
requests==2.31.0

# Note: Heavy ML dependencies (torch, opencv, etc.) will be installed at runtime
# to avoid build memory issues
